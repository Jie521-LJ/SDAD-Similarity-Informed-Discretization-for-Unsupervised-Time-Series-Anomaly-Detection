# SDAD VQ Backbone

This directory keeps the VQ-VAE backbone files required by the SDAD anomaly detection pipeline.

The runnable entry points are in the package root:

```text
run_reproduce.sh
run_reproduce.ps1
```

Only the files needed for the single-stage VQ reconstruction pipeline are retained.
